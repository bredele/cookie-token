

/**
 * Create a cookie session from a JWT token.
 *
 * @param {String} token
 * @param {String?} secret
 * @api public
 */

module.exports = function (token, secret = process.env.JWT_SECRET) {
  // do something
}
